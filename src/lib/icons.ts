/**
 * Reexporta `icons.tsx` (JSX no `Dot`). Ícones vêm de `react-icons/lu`, exceto `FaWhatsapp` (fa6).
 * Mantém este arquivo para resolução/cache que ainda aponta para `icons.ts`.
 */
export * from "./icons.tsx";
